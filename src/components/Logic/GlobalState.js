import React,{useState,useReducer} from 'react'

const GlobalState = () => {
    const [state,dispacth]=useReducer(reducer,initilState);

    function deleteIngredent(id){
        
    }
    return (
        <div>
            
        </div>
    )
}

export default GlobalState
